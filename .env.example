# Example environment variables for Recipe Tracker

# Port the Flask/Gunicorn app will listen on
PORT=5000

# Where recipe JSON data is stored inside the container
DATA_PATH=/app/data

# Where uploaded images are stored inside the container
UPLOAD_FOLDER=/app/uploads

# Flask environment
FLASK_ENV=production
